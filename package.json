{
  "dependencies": {
    "firebase": "^12.9.0"
  },
  "scripts": {
    "deploy": "cd frontend && npm run build && cd .. && npx wrangler pages deploy frontend/build --project-name=azylweb --branch=main"
  }
}