# No frontend env vars needed — Facebook credentials are stored
# as Cloudflare Pages environment variables (server-side only).
#
# For local development with wrangler, create a .dev.vars file:
#   FACEBOOK_ALBUM_ID=your_album_id
#   FACEBOOK_ACCESS_TOKEN=your_token
